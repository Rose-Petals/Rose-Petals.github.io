monday_temperatures = [9.1, 8.8, 7.5]
student_grades = {"Mary": 9.1, "Sim": 8.8, "JOhn": 7.5}

mySum = sum(student_grades.values())
length = len(student_grades)
mean = mySum/length
print(mean)

#use listname.index(number) to get the index of a particular number
#use listname[i] to get the value at a particular index
# xan use listname[starting index:ending index] to get a portion of a the list


#items in python lists have 2 indexes 0-n with 0 being the index of the first number and n-(-1) with -1 being the index of the last number and decreasing as you move backwards in a list


#use "clr" to clear command line(left)
#to clear terminal use "ctrl"+"L"

#use "exit()" to go to go back to terminal and use "python3" to go back to the shell
